/**
 * cas utilisation: (ADAPTER):
 *
 * + + Interface ou class a une fonction reutilisable mais non compatible (parametre ou valeur de retour) au nouveau fonctionnalité.
 *
 *      Les données(ou code) actuel ne corresponds pas à son paramètre (ou son sortie).
 *
 *      On doit faire une conversion ou some calcule pour pouvoir la reutiliser.
 */
package com.patterns.structural.adapter;